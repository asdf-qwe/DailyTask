custom:
  prod:
    db:
      password: ${DB_PASSWORD}
  jwt:
    secretKey: ${JWT_SECRET}

  accessToken:
    expirationSeconds: '#{60 * 10}'
  refreshToken:
    expirationSeconds: '#{60 * 60 * 24 * 90}'